# Google SSO via Supabase Auth

All authentication is handled through Google SSO using Supabase Auth. Email/password login is not offered. Xomnia staff already have Google Workspace accounts, so SSO avoids password management overhead and keeps access naturally tied to Xomnia's existing identity provider.
